<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Bruna Thomaz | Links Oficiais</title>
  <link rel="stylesheet" href="style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>

  <canvas id="fireworks"></canvas>

  <header>
    <h1>Bruna Thomaz</h1>
    <p> Criador de Conteúdo </p>


    <p> Me Siga Nas Redes Sociais! </p>
  </header>

  <div class="links">
    <a href="https://www.instagram.com/eibrinathomaz" target="_blank"><i class="fab fa-instagram"></i> Instagram</a>
    <a href="https://www.youtube.com/@Anurb_0" target="_blank"><i class="fab fa-youtube"></i> YouTube</a>
    <a href="https://www.twitch.tv/anurb_fx" target="_blank"><i class="fab fa-twitch"></i> Twitch</a>
    <a href="https://www.tiktok.com/@anurb1" target="_blank"><i class="fab fa-tiktok"></i> TikTok</a>
    <a href="https://discord.gg/jtswnVyt" target="_blank"><i class="fab fa-discord"></i> Discord</a>
    <a href="https://open.spotify.com/user/brunathomazfx" target="_blank"><i class="fab fa-spotify"></i> Spotify</a>
  </div>









  
  <footer>
    <p>© 2025 Bruna Thomaz. Todos os direitos reservados.</p>
  </footer>

  <script src="script.js"></script>
</body>
</html>

/* Estilo Geral */
body {
    margin: 0;
    font-family: 'Segoe UI', sans-serif;
    background: #0d011a;
    color: #fff;
    overflow-x: hidden;
  }
  
  header {
    text-align: center;
    padding: 50px 20px 20px;
    position: relative;
    z-index: 2;
  }
  
  header img {
    width: 150px;
    border-radius: 50%;
    border: 4px solid #a94bd1;
    margin-bottom: 15px;
  }
  
  h1 {
    font-size: 28px;
    color: #ffffff;
    margin: 0;
  }
  
  p {
    color: #d9a0ff;
    font-size: 16px;
  }
  
  .links {
    text-align: center;
    margin: 20px 0;
    position: relative;
    z-index: 2;
  }
  
  .links a {
    display: block;
    background: #290030;
    color: #fff;
    padding: 15px;
    margin: 12px auto;
    width: 90%;
    max-width: 400px;
    border-radius: 10px;
    text-decoration: none;
    font-size: 18px;
    border: 2px solid #a94bd1;
    transition: 0.3s ease;
  }
  
  .links a:hover {
    background: #520060;
    border-color: #ff6ec7;
  }
  
  .links i {
    margin-right: 10px;
  }
  
  .pix-section {
    text-align: center;
    margin-top: 40px;
    position: relative;
    z-index: 2;
  }
  
  .pix-button {
    display: inline-block;
    background: #a94bd1;
    color: white;
    padding: 14px 24px;
    font-size: 18px;
    font-weight: bold;
    border-radius: 10px;
    text-decoration: none;
    transition: background 0.3s ease;
    margin-top: 15px;
    border: none;
  }
  
  .pix-button:hover {
    background: #ff6ec7;
  }
  
  footer {
    margin-top: 50px;
    font-size: 14px;
    color: #999;
    position: relative;
    z-index: 2;
  }
  
  canvas#fireworks {
    position: fixed;
    top: 0;
    left: 0;
    z-index: 0;
    pointer-events: none;
  }
const canvas = document.getElementById('fireworks');
const ctx = canvas.getContext('2d');
let w = canvas.width = window.innerWidth;
let h = canvas.height = window.innerHeight;

window.addEventListener('resize', () => {
  w = canvas.width = window.innerWidth;
  h = canvas.height = window.innerHeight;
});

let particles = [];

function createFirework() {
  const x = Math.random() * w;
  const y = Math.random() * h / 2;
  const count = 80;
  for (let i = 0; i < count; i++) {
    const angle = (Math.PI * 2 * i) / count;
    const speed = Math.random() * 5 + 2;
    particles.push({
      x,
      y,
      vx: Math.cos(angle) * speed,
      vy: Math.sin(angle) * speed,
      alpha: 1,
      color: `hsl(${Math.random() * 360}, 100%, 70%)`
    });
  }
}

function updateFireworks() {
  ctx.clearRect(0, 0, w, h);
  particles = particles.filter(p => p.alpha > 0);
  for (let p of particles) {
    p.x += p.vx;
    p.y += p.vy;
    p.alpha -= 0.015;

    ctx.globalAlpha = p.alpha;
    ctx.fillStyle = p.color;
    ctx.beginPath();
    ctx.arc(p.x, p.y, 2, 0, Math.PI * 2);
    ctx.fill();
  }
  ctx.globalAlpha = 1;
}

function loop() {
  updateFireworks();
  requestAnimationFrame(loop);
}

setInterval(createFirework, 1000);
loop();
